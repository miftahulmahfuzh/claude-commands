{
  "extraPaths": [
    "skills/task"
  ],
  "//": "sync_todos.py imports taskcore/task_gl/todos from the DEPLOYED task skill at runtime (sys.path.insert on ~/.claude/skills/task). extraPaths points the checker at the repo copies -- the same files sync.sh deploys.",
  "exclude": [
    "**/__pycache__"
  ]
}
